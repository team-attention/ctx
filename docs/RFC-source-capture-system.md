# RFC: Source Capture System

> CTX 외부 데이터 소스 통합 제안서
> 작성일: 2026-01-03
> 작성자: Claude (Opus 4.5) + Agent Council (Codex, Gemini)

---

## 1. 배경 및 문제 정의

### 현재 상황 (As-Is)

CTX는 파일 기반 context 관리를 지원하지만, **외부 데이터 소스**에서 지식을 수집하는 기능이 없음:

```
현재 CTX가 다루는 것:
✅ 파일시스템 기반 context (.ctx/, *.ctx.md)
✅ CLI를 통한 context CRUD (create, load, save, sync)
✅ Claude Code Plugin을 통한 AI 연동

현재 CTX가 다루지 못하는 것:
❌ Slack 메시지/스레드
❌ GitHub 이슈/PR/커밋
❌ Claude Code 세션 히스토리
❌ 기타 외부 데이터 소스
```

### 문제점

| 문제 | 설명 |
|------|------|
| **지식 유실** | Slack에서 논의된 결정사항이 context로 저장되지 않음 |
| **수동 복사** | 외부 데이터를 context로 옮기려면 수동 복사 필요 |
| **단절된 흐름** | 세션 히스토리가 다음 세션으로 이어지지 않음 |
| **분산된 지식** | 팀 지식이 Slack, GitHub, 문서 등에 흩어져 있음 |

### 핵심 질문

> "다양한 소스에서 데이터를 가져와 CTX context로 자동 저장할 수 있을까?"

---

## 2. 설계 원칙

### Council 합의 사항 (Codex + Gemini)

Agent Council 논의를 통해 다음 원칙에 합의:

1. **CLI 불변 원칙**: `ctx save`는 파일 저장만 담당, source 로직 넣지 않음
2. **MCP 재사용**: 기존 MCP 서버 활용 (Slack, GitHub 등), 직접 구현 최소화
3. **Skill 기반 모듈화**: 소스별 Skill로 분리, 재사용성 확보
4. **중간 저장소 (inbox)**: raw 데이터 임시 저장 → 디버깅/재처리 용이
5. **AI Orchestration**: Agent가 Skill 조합하여 복잡한 워크플로우 처리

### 기술적 제약 (2026-01 기준)

GitHub 이슈 조사 결과 확인된 제약:

| 기능 | 상태 | 관련 이슈 |
|------|------|-----------|
| Agent별 MCP 동적 로드 | ❌ 미지원 | #13700, #13805 |
| Agent만 MCP 접근 | ❌ 미지원 | #6915, #4476 |
| Custom Agent MCP 접근 | ⚠️ 버그 | #13605 |
| Task 병렬 실행 | ✅ 지원 | - |

**결론**: Skill 기반 접근이 현재 가장 안정적

---

## 3. 제안 아키텍처

### 전체 구조

```
┌─────────────────────────────────────────────────────────────┐
│  User Request                                               │
│  "슬랙이랑 깃헙에서 어제 내용 가져와서 정리해줘"              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  ctx-capture Agent (Orchestrator)                           │
│  • 요청 분석 → 필요한 skill 결정                             │
│  • Skill 순차 호출 (참조)                                    │
│  • inbox 결과 종합 → 인사이트 추출                           │
│  • ctx save 호출                                            │
└─────────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┴─────────────────┐
         ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│  slack-capture  │               │ session-capture │
│     Skill       │               │     Skill       │
│                 │               │                 │
│  Slack MCP      │               │ ~/.claude/      │
│  호출 가이드    │               │ projects/ 읽기  │
└────────┬────────┘               └────────┬────────┘
         │                                 │
         ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│  Slack MCP      │               │  Read (JSONL)   │
│  mcp__slack__*  │               │  세션 파일 분석 │
└─────────────────┘               └─────────────────┘
         │                                 │
         └─────────────────┬───────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  .ctx/inbox/ (중간 저장소)                                   │
│  ├── slack/2026-01-03.json                                  │
│  └── session/2026-01-03.json                                │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  ctx save (자유 경로)                                        │
│  사용자가 원하는 경로에 최종 context 저장                     │
└─────────────────────────────────────────────────────────────┘
```

### 컴포넌트 역할

| 컴포넌트 | 역할 | 위치 |
|----------|------|------|
| **ctx-capture Agent** | Orchestrator, 여러 소스 조합 | `plugin/agents/ctx-capture/` |
| **slack-capture Skill** | Slack MCP 사용 가이드 | `plugin/skills/slack-capture/` |
| **session-capture Skill** | Claude Code 세션 히스토리 분석 | `plugin/skills/session-capture/` |
| **/ctx.capture 명령어** | 사용자 진입점 | `plugin/commands/capture.md` |
| **inbox** | 중간 저장소 (raw 데이터) | `.ctx/inbox/<source>/` |

---

## 4. 플러그인 파일 구조

```
plugin/
├── .claude-plugin/
│   └── plugin.json
│
├── .mcp.json                    # MCP 서버 등록 (전역)
│
├── agents/
│   └── ctx-capture/
│       └── AGENT.md             # Master orchestrator
│
├── skills/
│   ├── slack-capture/
│   │   └── SKILL.md             # Slack MCP 사용 가이드
│   │
│   ├── session-capture/
│   │   └── SKILL.md             # Claude Code 세션 히스토리 분석
│   │
│   ├── ctx-load/                # 기존
│   └── ctx-save/                # 기존
│
├── commands/
│   └── capture.md               # /ctx.capture 명령어
│
└── shared/
    └── inbox-schema.md          # inbox 저장 형식 정의
```

---

## 5. 상세 설계

### 5.1 MCP 설정 (`.mcp.json`)

```json
{
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-slack"],
      "env": {
        "SLACK_TOKEN": "${SLACK_TOKEN}"
      }
    }
  }
}
```

**참고**: GitHub은 `gh` CLI 사용 권장 (더 안정적)

### 5.2 ctx-capture Agent

```yaml
---
name: ctx-capture
description: "데이터 캡처", "소스에서 가져와", "슬랙/깃헙 저장" 요청 시 사용
---
```

**역할**:
1. 사용자 요청 분석 → 필요한 source 결정
2. 해당 capture skill 참조하여 데이터 수집
3. `.ctx/inbox/<source>/`에 raw 데이터 저장
4. inbox 결과 종합하여 인사이트 추출
5. `ctx save`로 최종 context 저장

### 5.3 Source Capture Skills

#### slack-capture Skill

```yaml
---
name: slack-capture
description: "슬랙 가져와", "슬랙 메시지 캡처" 요청 시 사용
allowed-tools: mcp__slack__*, Write
---
```

**사용 MCP 도구**:
- `mcp__slack__slack_list_channels` - 채널 목록
- `mcp__slack__slack_get_channel_history` - 채널 메시지
- `mcp__slack__slack_get_thread_replies` - 스레드 답글

#### session-capture Skill

```yaml
---
name: session-capture
description: "세션 기록 가져와", "오늘 작업 내용", "피드백 추출" 요청 시 사용
allowed-tools: Read, Glob, Bash(cat:*), Write
---
```

**데이터 위치**: `~/.claude/projects/<encoded-cwd>/*.jsonl`

**경로 인코딩 규칙**:
- `/` → `-`
- 예: `/Users/hoyeonlee/team-attention/ctx` → `-Users-hoyeonlee-team-attention-ctx`

**세션 파일 구조** (JSONL):
```json
{"type":"user","message":{"role":"user","content":"..."}}
{"type":"assistant","message":{"role":"assistant","content":"..."}}
```

**사용 패턴 예시**:
1. "해당 세션에서 내가 남긴 주요 피드백 추출해줘"
2. "오늘 작업한 것들 중 terraform 관련 인사이트 정리해줘"
3. "이번 주 ctx 프로젝트 작업 요약해줘"

> **구현 시 주의**: 스코프를 현재 프로젝트(cwd)로만 제한하지 말 것!
> - 기본: 현재 프로젝트
> - "모든 프로젝트에서..." → 전체 검색
> - "terraform 프로젝트에서..." → 특정 프로젝트 검색
>
> 사용자 요청에 따라 유연하게 스코프 결정

### 5.4 inbox 스키마

#### Slack inbox
```json
{
  "source": "slack",
  "fetched_at": "2026-01-03T10:00:00Z",
  "query": {
    "channel": "#team-ai",
    "since": "2026-01-02"
  },
  "data": {
    "messages": [...]
  },
  "metadata": {
    "count": 42
  }
}
```

#### Session inbox
```json
{
  "source": "session",
  "fetched_at": "2026-01-03T10:00:00Z",
  "query": {
    "project": "/Users/hoyeonlee/team-attention/ctx",
    "date": "2026-01-03",
    "filter": "terraform"
  },
  "data": {
    "sessions": [
      {
        "session_id": "60394218-...",
        "messages": [...]
      }
    ]
  },
  "metadata": {
    "session_count": 3,
    "message_count": 150
  }
}
```

**저장 위치**: `.ctx/inbox/<source>/<date>.json`
**보관 정책**: `.gitignore`에 추가, 임시 데이터로 취급

### 5.5 최종 저장

**ctx save로 자유 경로에 저장** (별도 폴더 구조 강제 없음)

```bash
# 예시 - 사용자/Agent가 원하는 경로로 저장
ctx save --path slack-summary-2026-01-03.md --content "..."
ctx save --path daily/2026-01-03.md --content "..."
ctx save --path notes/team-meeting.md --content "..."
```

**inbox vs 최종 저장 비교**:

| | **inbox** | **ctx save** |
|---|---|---|
| 역할 | 중간 저장소 (RAW) | 최종 context |
| 형식 | JSON | Markdown |
| 위치 | `.ctx/inbox/` (고정) | 자유 경로 |
| Git | ❌ .gitignore | ✅ 커밋 가능 |
| 수명 | 임시 | 영구 |

---

## 6. 워크플로우

### 6.1 단일 소스 (간단)

```
User: "슬랙 #general 어제 대화 저장해"
                │
                ▼
        Main Claude
                │
                ▼
    Skill(slack-capture) 참조
                │
                ▼
    mcp__slack__slack_get_channel_history
                │
                ▼
    .ctx/inbox/slack/2026-01-03.json 저장
                │
                ▼
    ctx save --path slack-2026-01-03.md  # 자유 경로
```

### 6.2 복합 소스 (Agent 사용)

```
User: "슬랙이랑 오늘 세션 기록 종합해서 저장해"
                │
                ▼
    Task(ctx-capture Agent)
                │
    ┌───────────┴───────────┐
    ▼                       ▼
slack-capture 참조    session-capture 참조
    │                       │
    ▼                       ▼
Slack MCP 호출        ~/.claude/projects/ 읽기
    │                       │
    ▼                       ▼
inbox/slack/           inbox/session/
    │                       │
    └───────────┬───────────┘
                │
                ▼
    Agent가 inbox 읽고 종합 인사이트 추출
                │
                ▼
    ctx save --path daily-summary-2026-01-03.md  # 자유 경로
```

### 6.3 세션 기반 요청 예시

```
User: "오늘 작업하면서 terraform 관련해서 배운 것들 정리해줘"
                │
                ▼
        Main Claude
                │
                ▼
    Skill(session-capture) 참조
                │
                ▼
    ~/.claude/projects/<cwd>/에서 오늘 세션 파일 찾기
                │
                ▼
    JSONL 파싱 → terraform 관련 내용 필터링
                │
                ▼
    .ctx/inbox/session/2026-01-03.json 저장
                │
                ▼
    인사이트 추출 → ctx save --path terraform-learnings.md
```

### 6.4 /ctx.capture 명령어

```bash
/ctx.capture slack #team-ai 어제
/ctx.capture session 오늘 terraform
/ctx.capture all 오늘
```

---

## 7. 구현 계획

### Phase 1: 기반 구축
- [ ] `.mcp.json` 생성 (Slack MCP 등록)
- [ ] `inbox/` 디렉토리 구조 및 `.gitignore` 설정
- [ ] `shared/inbox-schema.md` 작성

### Phase 2: Skill 구현
(skill development 스킬을 사용해서 만들어라)
- [ ] `skills/slack-capture/SKILL.md` 작성
- [ ] `skills/session-capture/SKILL.md` 작성

### Phase 3: Agent 구현
(agent development 스킬을 사용해서 만들어라)
- [ ] `agents/ctx-capture/AGENT.md` 작성
- [ ] 인사이트 추출 로직 정의

### Phase 4: 명령어 및 문서화
(command development 스킬을 사용해서 만들어라)
- [ ] `commands/capture.md` 작성
- [ ] AGENTS.md 업데이트
- [ ] README 업데이트

---

## 8. 향후 고려사항

### 8.1 자동화 (Hooks)

SessionEnd Hook으로 세션 종료 시 자동 캡처:

```yaml
# hooks/session-end.md
---
event: SessionEnd
---
세션 종료 시 주요 내용을 자동으로 캡처합니다.
ctx-capture agent를 호출하여 session-capture를 실행합니다.
```

**현재는 수동 실행으로 시작, 안정화 후 Hook 추가**

### 8.2 추가 소스

확장 가능한 소스 후보:
- Linear 이슈
- Notion 페이지
- Google Docs
- Jira 이슈
- Discord 채널

### 8.3 병렬 실행 (미래)

Claude Code에서 Agent별 MCP 동적 로드가 지원되면:
- Skill → Agent로 전환
- Task 병렬 호출로 성능 개선

관련 GitHub 이슈:
- [#13700](https://github.com/anthropics/claude-code/issues/13700) - Lazy-load MCP servers per agent
- [#6915](https://github.com/anthropics/claude-code/issues/6915) - MCP tools only to subagent (184+ 👍)

---

## 9. 결론

### 핵심 결정

1. **Skill 기반 접근**: 현재 가장 안정적, MCP 버그 회피
2. **2개 소스로 시작**: Slack (MCP), Session (로컬 파일)
3. **inbox 패턴**: 중간 저장소로 디버깅/재현성 확보
4. **Agent Orchestration**: 복합 요청은 ctx-capture Agent가 처리

### 장점

| 측면 | 이점 |
|------|------|
| **재사용성** | Skill을 Agent/Master 모두에서 사용 |
| **모듈화** | 소스별 Skill 독립적 관리 |
| **디버깅** | inbox에 중간 결과 저장 |
| **확장성** | 새 소스 = 새 Skill 추가 |
| **안정성** | 검증된 MCP/CLI 활용 |

### 참고 자료

- [Claude Code MCP Integration](https://code.claude.com/docs/en/mcp)
- [Claude Code Hooks Reference](https://code.claude.com/docs/en/hooks)
- [Slack MCP Server](https://glama.ai/mcp/servers/@modelcontextprotocol/slack)
- Agent Council 논의 (2026-01-03)
